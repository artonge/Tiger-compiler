("test";
(kikoo());
let
	var str := "te t"

	var i := 87

	function main(test:int, ok :string, deux: char) : int = (
    while i <> a+b do print("test", a, 2);

    if i < 4 then
      print("ok")
    else (
      print("ok");
			x := -1+aze()+a-1;
			return x
		);

		let
			var in_scope_2 : int := 3
		in
			prout()
		end;

		return x
  )

  var a : int := 1+4*56/9*(3+a)+34

in
	for i:=0 to func(89) do main(test, 2+1, "ok")
end
)
